defmodule Zenith.Repo.Migrations.EnrollmentOnResult do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:results) do
      add :enrollment_id,
          references(:enrollments,
            column: :id,
            name: "results_enrollment_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false
    end
  end

  def down do
    drop constraint(:results, "results_enrollment_id_fkey")

    alter table(:results) do
      remove :enrollment_id
    end
  end
end

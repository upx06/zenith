defmodule Zenith.Repo.Migrations.ResultIdentity do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create unique_index(:results, [:exam_id, :enrollment_id],
             name: "results_result_identity_index"
           )
  end

  def down do
    drop_if_exists unique_index(:results, [:exam_id, :enrollment_id],
                     name: "results_result_identity_index"
                   )
  end
end

defmodule Zenith.Repo.Migrations.RemovingExamResultRelationshipAndResultIdentity do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:results) do
      remove :exam_id
    end

    drop_if_exists unique_index(:results, [:exam_id, :enrollment_id],
                     name: "results_result_identity_index"
                   )
  end

  def down do
    create unique_index(:results, [:exam_id, :enrollment_id],
             name: "results_result_identity_index"
           )

    alter table(:results) do
      add :exam_id,
          references(:exams,
            column: :id,
            name: "results_exam_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false
    end
  end
end
